const profile = (req, res) => {
    // if (req.body.username === 'andrea') return res.send({ status: true });
    // else {
    //     return res.send({ status: false });
    // }
};

export { profile };
